## 2026-02-08 - [RTL Support for Input Icons]
**Learning:** Using `pe-10` (padding-end) instead of `pr-10` ensures icon-containing inputs align correctly in both RTL (Arabic) and LTR layouts.
**Action:** Always use logical properties for spacing inside interactive components.
