## 2024-05-22 - Adding Loading States to Buttons
**Learning:** Adding `isLoading` to `Button` component significantly improves UX by preventing duplicate submissions and giving immediate feedback. `aria-busy` is critical for accessibility.
**Action:** Always check if a `Button` component supports loading states and implement it if missing.
