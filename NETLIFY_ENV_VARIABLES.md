# متغيرات البيئة المطلوبة في Netlify

هذا الملف يحتوي على جميع متغيرات البيئة التي تحتاج إلى إضافتها في Netlify Dashboard.

## 📝 الخطوات

1. في Netlify Dashboard، افتح موقعك
2. انتقل إلى **Site settings** → **Environment variables**
3. أضف المتغيرات التالية:

---

## 🔐 المتغيرات المطلوبة

### 1. DATABASE_URL

**المفتاح:** `DATABASE_URL`

**القيمة:**
```
mongodb+srv://mohammedemad123me_db_user:Q74dIpOP8Vkd0bOP@cluster0.10zcwzy.mongodb.net/medical_education?retryWrites=true&w=majority
```

**ملاحظة:** استبدل `medical_education` باسم قاعدة البيانات التي تريد استخدامها.

---

### 2. JWT_SECRET

**المفتاح:** `JWT_SECRET`

**القيمة:** يجب أن تكون مفتاح سري قوي بطول 32 حرف على الأقل.

**مثال:**
```
your-super-secret-jwt-key-here-minimum-32-characters-long
```

**إنشاء مفتاح قوي:**
```bash
# في Terminal (Linux/Mac)
openssl rand -base64 32

# أو استخدم مولد كلمات مرور آمنة
# يجب أن يحتوي على: أحرف كبيرة، أحرف صغيرة، أرقام، ورموز
```

**مثال على مفتاح قوي:**
```
Xk8pL9mN2qR5sT7vW0yZ3aB6cD8eF1gH4jK7lM0nP3qR6sT9vW2yZ5a
```

⚠️ **تحذير:** لا تشارك هذا المفتاح أبداً!

---

### 3. NODE_ENV

**المفتاح:** `NODE_ENV`

**القيمة:**
```
production
```

**ملاحظة:** هذا مهم لأداء Next.js وإعدادات الأمان.

---

## 📋 ملخص سريع

انسخ والصق في Netlify Environment Variables:

```
DATABASE_URL=mongodb+srv://mohammedemad123me_db_user:Q74dIpOP8Vkd0bOP@cluster0.10zcwzy.mongodb.net/medical_education?retryWrites=true&w=majority
JWT_SECRET=your-super-secret-jwt-key-here-minimum-32-characters-long
NODE_ENV=production
```

---

## 🔄 إعدادات إضافية (اختياري)

### 4. NEXT_PUBLIC_SITE_URL (اختياري)

**المفتاح:** `NEXT_PUBLIC_SITE_URL`

**القيمة:**
```
https://your-site-name.netlify.app
```

أو اسم النطاق المخصص:
```
https://yourdomain.com
```

**ملاحظة:** هذا مهم إذا كنت تستخدم هذا المتغير في الكود.

---

## ✅ التحقق من الإعدادات

بعد إضافة المتغيرات:

1. **Redeploy** الموقع من Netlify Dashboard:
   - **Deploys** → **Trigger deploy** → **Deploy site**
   
2. تحقق من **Build Log**:
   - يجب أن ترى `Prisma Client` يتم توليده بنجاح
   - يجب ألا توجد أخطاء في الاتصال بقاعدة البيانات

3. اختبر الموقع:
   - افتح الموقع المنشور
   - جرب تسجيل الدخول
   - تأكد من أن API routes تعمل

---

## 🐛 حل المشاكل

### مشكلة: خطأ في الاتصال بقاعدة البيانات

**الحل:**
1. تحقق من أن `DATABASE_URL` صحيح
2. تأكد من أن MongoDB Atlas يسمح بعناوين IP الخاصة بـ Netlify
3. في MongoDB Atlas → **Network Access** → أضف `0.0.0.0/0` للسماح للجميع

### مشكلة: خطأ JWT_SECRET

**الحل:**
1. تأكد من أن `JWT_SECRET` بطول 32 حرف على الأقل
2. لا تضع مسافات إضافية في البداية أو النهاية
3. استخدم مفتاح جديد وقوي

### مشكلة: Prisma Client لم يتم توليده

**الحل:**
1. تأكد من أن `DATABASE_URL` موجود في Environment Variables
2. راجع Build Log في Netlify
3. تأكد من أن `postinstall` script موجود في `package.json`

---

**آخر تحديث:** يناير 2025
