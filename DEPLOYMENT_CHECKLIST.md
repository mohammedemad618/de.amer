# ✅ قائمة التحقق من النشر على Netlify

استخدم هذه القائمة للتأكد من اكتمال جميع الخطوات قبل النشر.

## 📋 قبل النشر

### 1. إعداد MongoDB Atlas
- [ ] إنشاء cluster في MongoDB Atlas
- [ ] إنشاء مستخدم قاعدة بيانات
- [ ] الحصول على رابط الاتصال (Connection String)
- [ ] إضافة اسم قاعدة البيانات إلى رابط الاتصال
- [ ] السماح بعناوين IP (0.0.0.0/0 أو IP Netlify)

### 2. إعداد المشروع محلياً
- [ ] تحديث `prisma/schema.prisma` لـ MongoDB ✅
- [ ] تحديث `package.json` بإضافة `postinstall` ✅
- [ ] تحديث `src/lib/db/prisma.ts` ✅
- [ ] إنشاء `netlify.toml` ✅
- [ ] تحديث `.gitignore` (لا يتضمن ملفات حساسة) ✅

### 3. اختبار محلي
- [ ] تشغيل `npm install`
- [ ] تشغيل `npx prisma generate`
- [ ] تشغيل `npx prisma db push`
- [ ] تشغيل `npm run prisma:seed` (لإضافة بيانات تجريبية)
- [ ] تشغيل `npm run dev`
- [ ] اختبار جميع الصفحات محلياً
- [ ] اختبار تسجيل الدخول
- [ ] اختبار API routes

### 4. إعداد Git
- [ ] التأكد من وجود `.gitignore`
- [ ] التأكد من عدم وجود ملفات حساسة (`.env`) في Git
- [ ] عمل commit لجميع التغييرات
- [ ] رفع الكود إلى Git (GitHub/GitLab/Bitbucket)

## 🚀 أثناء النشر

### 5. إعداد Netlify
- [ ] إنشاء حساب Netlify
- [ ] ربط Git repository مع Netlify
- [ ] اختيار Branch (main/master)
- [ ] التحقق من Build Settings:
  - [ ] Build command: `npm run build`
  - [ ] Publish directory: `.next` (أو auto-detect)

### 6. إضافة متغيرات البيئة في Netlify
- [ ] `DATABASE_URL` (مع اسم قاعدة البيانات)
- [ ] `JWT_SECRET` (32+ حرف)
- [ ] `NODE_ENV` = `production`
- [ ] أي متغيرات بيئة أخرى مطلوبة

### 7. البناء الأول
- [ ] بدء النشر في Netlify
- [ ] مراقبة Build Log
- [ ] التحقق من:
  - [ ] `prisma generate` يعمل بنجاح
  - [ ] `next build` يكتمل بدون أخطاء
  - [ ] لا توجد أخطاء TypeScript
  - [ ] لا توجد أخطاء ESLint

## ✅ بعد النشر

### 8. الاختبار
- [ ] فتح الموقع المنشور
- [ ] اختبار الصفحة الرئيسية
- [ ] اختبار صفحة الدورات
- [ ] اختبار صفحة تسجيل الدخول
- [ ] اختبار صفحة التسجيل
- [ ] اختبار API endpoints
- [ ] اختبار الصور والأصول الثابتة

### 9. إعداد قاعدة البيانات (إن لم يكن تم)
- [ ] تشغيل `npx prisma db push` محلياً (مع DATABASE_URL production)
- [ ] تشغيل `npm run prisma:seed` محلياً (إن أردت بيانات تجريبية)

### 10. إعدادات إضافية
- [ ] إعداد Custom Domain (إن كان متاحاً)
- [ ] التحقق من SSL Certificate (تلقائي في Netlify)
- [ ] إعداد Analytics (اختياري)
- [ ] إعداد Backups في MongoDB Atlas
- [ ] إعداد Alerts في MongoDB Atlas

## 🔐 الأمان

### 11. مراجعة الأمان
- [ ] التأكد من عدم وجود معلومات حساسة في الكود
- [ ] التأكد من أن جميع Environment Variables محمية
- [ ] مراجعة CSP Headers
- [ ] مراجعة CORS settings (إن لزم الأمر)
- [ ] مراجعة MongoDB Network Access

## 📊 المراقبة

### 12. إعداد المراقبة
- [ ] تفعيل Netlify Analytics (اختياري)
- [ ] إعداد MongoDB Atlas Monitoring
- [ ] إعداد Alerts للأخطاء
- [ ] مراجعة Function Logs في Netlify

## 📝 الوثائق

### 13. الوثائق
- [ ] قراءة `NETLIFY_DEPLOYMENT.md` ✅
- [ ] قراءة `NETLIFY_ENV_VARIABLES.md` ✅
- [ ] توثيق أي إعدادات مخصصة

---

## 🎉 النشر مكتمل!

إذا أكملت جميع العناصر أعلاه، موقعك جاهز للاستخدام.

## 🆘 في حالة وجود مشاكل

راجع:
1. **Build Log** في Netlify Dashboard
2. **Function Logs** للـ API routes
3. **MongoDB Atlas Logs**
4. **NETLIFY_DEPLOYMENT.md** → قسم "حل المشاكل الشائعة"

---

**تاريخ النشر:** _______________
**النسخة:** _______________
